import { Message } from "discord.js";

// {{name}}
export default async (message: Message) => {
  // TODO return true if handled and should stop
  // TODO don't forget to include this module in `src/events/messageCreate/handlers/index.ts`
  return false;
};
